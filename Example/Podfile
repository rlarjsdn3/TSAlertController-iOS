use_frameworks!

platform :ios, '15.0'

target 'TSAlertController_Example' do
  pod 'TSAlertController', :path => '../'

  target 'TSAlertController_Tests' do
    inherit! :search_paths

    
  end
end
